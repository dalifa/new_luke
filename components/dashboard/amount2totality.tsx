/*
"use client";

import { amount2Totality } from "@/actions/amount2Totality";

interface Amount2TotalityButtonProps {
  children?: React.ReactNode;
};

export const Amount2TotalityButton = ({
  children
}: Amount2TotalityButtonProps) => {
  const onClick2Totality = () => {
    amount2Totality();
  };

  return (
    <div onClick={onClick2Totality} className="cursor-pointer bg-blue-600 text-white text-center rounded p-1 md:p-2 hover:bg-green-700">
      {children}
    </div>
  ); 
};
*/