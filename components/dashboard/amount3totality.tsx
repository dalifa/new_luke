/*
"use client";

import { amount3Totality } from "@/actions/amount3Totality";

interface Amount3TotalityButtonProps {
  children?: React.ReactNode;
};

export const Amount3TotalityButton = ({
  children
}: Amount3TotalityButtonProps) => {
  const onClick3Totality = () => {
    amount3Totality();
  };

  return (
    <div onClick={onClick3Totality} className="cursor-pointer bg-blue-600 text-white text-center rounded p-1 md:p-2 hover:bg-green-700">
      {children}
    </div>
  ); 
};
*/