/*
"use client";

import { amount1Totality } from "@/actions/amount1Totality";

interface Amount1TotalityButtonProps {
  children?: React.ReactNode;
};

export const Amount1TotalityButton = ({
  children
}: Amount1TotalityButtonProps) => {
  const onClick1Totality = () => {
    amount1Totality();
  };

  return (
    <div onClick={onClick1Totality} className="cursor-pointer bg-blue-600 text-white text-center rounded p-1 md:p-2 hover:bg-green-700">
      {children}
    </div>
  ); 
};
 */